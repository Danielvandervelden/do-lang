# Intake Transcript — skill-tree-dedup

**Session:** 2026-05-04T13:17:13Z
**Pass:** 1 + 2 (Combined)

---

## Pass 1 — Vision Questions

### Q1: Product Vision
**Q:** Describe the project in one paragraph: what it is, who it's for, and the core problem it solves.
**A:** Consolidate the duplicated Claude and Codex skill/agent/reference markdown trees in do-lang into a single canonical source with a build step that generates platform-specific variants at install time. The project is for the do-lang maintainer (Daniel), solving the maintenance cost of keeping ~120 nearly-identical files in sync across two platform targets.

### Q2: Primary User
**Q:** Who are the primary users? What's their technical level and how will they interact with the product?
**A:** The primary user is Daniel (sole maintainer of do-lang). Technical level: expert. Interaction: editing canonical skill/agent files and running the build step during development/release.

### Q3: Non-Goals
**Q:** What is explicitly OUT of scope for this project?
**A:** No new skills or behavior changes — structural consolidation only. No third platform targets — Claude + Codex only.

### Q4: Success Criteria
**Q:** What does success look like? List 3-5 measurable outcomes that define "done" for the project.
**A:** (1) Single source of truth — each skill/agent/reference exists in exactly one canonical file. (2) Both installs work identically — installClaude() and installCodex() produce identical runtime behavior to v1.19.0. (3) Measurably fewer .md files to maintain.

### Q5: Constraints
**Q:** What hard constraints exist?
**A:** Backward-compatible install — existing consumers running `npm install -g` must not break, postinstall output paths unchanged. Install flow should override existing installation. Preserve "do you want to install to claude, codex or both" prompt. Check if both CLIs are installed before asking (no need to ask about a target that isn't installed). No timeline pressure (personal project).

### Q6: Top Risks
**Q:** What are the top risks that could derail this project?
**A:** Build step complexity — template markers + build script become another thing to maintain. Divergence creep — over time, Claude and Codex variants may need genuinely different logic. Install breakage — consolidated install could silently break one platform.

### Q7: Integrations
**Q:** What external systems, APIs, or services will this project integrate with?
**A:** Claude Code CLI (install to ~/.claude/), Codex CLI (install to ~/.codex/). No external APIs.

### Q8: Non-Functional Requirements
**Q:** What non-functional requirements matter most?
**A:** Install speed — build step shouldn't noticeably slow down `npm install -g`. Maintainability is the primary non-functional concern.

### Q9: Existing Codebase
**Q:** Is there an existing codebase or greenfield?
**A:** Existing codebase. Node.js 18+, CommonJS modules (.cjs), npm package. Key directories: skills/do/ (Claude skills), skills/codex/ (Codex skills — duplication target), agents/ (do-*.md and codex-*.md), bin/install.cjs (postinstall script).

### Q10: MVP Milestone
**Q:** Is there a "minimum viable" milestone?
**A:** Shared references first — deduplicate the 47 reference files (identical content) before tackling entry skills and agents (which have platform-specific diffs).

---

## Confidence Assessment

| Factor | Score | Notes |
|--------|-------|-------|
| Context | 0.95 | Well-understood — maintainer's own project, clear problem statement |
| Scope | 0.90 | Clear MVP, clear non-goals, some open questions on exact template syntax |
| Complexity | 0.85 | Build step complexity is a real risk, need to decide exact template approach |
| Familiarity | 0.95 | Maintainer knows the codebase intimately |

**Overall Confidence:** 0.91
**Threshold:** 0.9

---

## Free-Form Notes

- User wants Codex council (GPT-5.5 preferred) involved in all planning and execution reviews.
- Council config already has `reviewer: "codex"` for both planning and execution. Model flag not yet passed to codex-companion — may need a follow-up fix.
- Backlog item `skill-dedup-research` in `.do/BACKLOG.md` is the source reference.
- Final phase should prompt user to run `/skill-creator` on each canonical skill (do not invoke automatically).
