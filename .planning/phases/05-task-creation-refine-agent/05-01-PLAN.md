---
phase: 05-task-creation-refine-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/do/scripts/load-task-context.cjs
  - skills/do/references/task-template.md
autonomous: true
requirements:
  - TS-06

must_haves:
  truths:
    - "Keyword extraction from task description produces relevant tech terms"
    - "Database folders (components/, tech/, features/) are searched for matches"
    - "Output includes project.md path, matched docs, and keywords"
    - "Task template has YAML frontmatter with all required stage and confidence fields"
    - "Task template body has sections for problem, context, approach, concerns"
  artifacts:
    - path: "skills/do/scripts/load-task-context.cjs"
      provides: "Keyword-based context loading for task refinement"
      exports: ["loadTaskContext"]
    - path: "skills/do/references/task-template.md"
      provides: "Task markdown template with YAML frontmatter placeholders"
      contains: ["{{CONFIDENCE_SCORE}}", "{{PROBLEM_STATEMENT}}", "{{CONTEXT_LOADED}}", "{{APPROACH}}", "{{CONCERNS}}"]
  key_links:
    - from: "skills/do/scripts/load-task-context.cjs"
      to: ".do-workspace.json"
      via: "findWorkspaceConfig() traversal"
      pattern: "findWorkspaceConfig"
    - from: "skills/do/scripts/load-task-context.cjs"
      to: "database/projects/<name>"
      via: "keyword matching against md files"
      pattern: "searchDirs.*components.*tech.*features"
---

<objective>
Create the foundational assets for task refinement: context loading script and task markdown template.

Purpose: These assets are prerequisites for the /do:task skill implementation. The context loader enables targeted context loading (D-07 to D-10), and the template defines the task markdown structure (D-01, D-02).

Output: Two files ready for use by /do:task skill in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-task-creation-refine-agent/05-CONTEXT.md
@.planning/phases/05-task-creation-refine-agent/05-RESEARCH.md

# Existing patterns to follow
@skills/do/scripts/check-database-entry.cjs
@skills/do/references/config-template.json
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From skills/do/scripts/check-database-entry.cjs:
```javascript
// Pattern: findWorkspaceConfig() traverses up to find .do-workspace.json
function findWorkspaceConfig(startPath) {
  let current = path.resolve(startPath);
  const root = path.parse(current).root;
  while (current !== root) {
    const configPath = path.join(current, '.do-workspace.json');
    if (fs.existsSync(configPath)) return configPath;
    current = path.dirname(current);
  }
  return null;
}

// Pattern: readJsonSafe() for safe file reading
function readJsonSafe(filePath) {
  const content = readFileSafe(filePath);
  if (!content) return null;
  try { return JSON.parse(content); } catch { return null; }
}
```

From .do/config.json schema (config-template.json):
```json
{
  "version": "0.1.0",
  "project_name": "{{PROJECT_NAME}}",
  "database_entry": false,
  "active_task": null,
  "auto_grill_threshold": 0.9,
  "council_reviews": { ... }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create load-task-context.cjs script</name>
  <files>skills/do/scripts/load-task-context.cjs</files>
  <behavior>
    - extractKeywords("Fix login form validation errors") returns ["form", "validation", "login", "errors"]
    - extractKeywords("Add DataGrid sorting") returns ["datagrid", "sorting"]
    - findMatchingDocs() searches components/, tech/, features/ folders for keyword matches
    - findMatchingDocs() returns empty array (not error) if no database folders exist
    - loadTaskContext() returns { project_md_path, matched_docs, keywords, database_path }
    - CLI: node load-task-context.cjs "task description" outputs JSON
    - CLI: --pretty flag pretty-prints JSON
    - CLI: --help shows usage
  </behavior>
  <action>
Create Node.js script following the check-database-entry.cjs pattern.

**Per D-09 (targeted context via keyword matching):**

1. `extractKeywords(description)`:
   - Common tech terms list: datagrid, table, form, input, select, autocomplete, authentication, auth, login, validation, api, endpoint, upload, download, modal, dialog, route, navigation, state, redux, store, hook, i18n, translation, error, loading, skeleton, pagination
   - Extract words from description, filter to: (a) matches tech terms OR (b) word length > 5
   - Return lowercase, deduplicated array

2. `findMatchingDocs(databasePath, keywords)`:
   - Search directories: components, tech, features
   - For each .md file in these dirs, check if any keyword appears in filename or content
   - Return array of absolute paths to matched docs
   - Return [] if directories don't exist (no error)

3. `loadTaskContext(projectPath, description)`:
   - Find workspace config via findWorkspaceConfig()
   - Read database path from workspace config
   - Read project name from .do/config.json
   - Build database path: <database>/projects/<project_name>
   - Extract keywords from description
   - Find matching docs in database folders
   - Return structured result

**CLI interface:**
- `node load-task-context.cjs "description"` - from cwd
- `node load-task-context.cjs "description" <project-path>` - specific project
- `--pretty` - pretty print JSON
- `--help` - show usage

**Error handling:**
- Exit 2 if workspace not initialized
- Exit 2 if project not initialized
- Always output JSON (even on error)

Follow existing script patterns: JSDoc comments, safe file reading, CLI arg parsing.
  </action>
  <verify>
    <automated>node skills/do/scripts/load-task-context.cjs --help && node skills/do/scripts/load-task-context.cjs "Fix login form validation" --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Script outputs JSON with project_md_path, matched_docs, keywords, database_path fields</done>
</task>

<task type="auto">
  <name>Task 2: Create task-template.md reference file</name>
  <files>skills/do/references/task-template.md</files>
  <action>
Create task markdown template per D-01 (comprehensive problem statements) and D-02 (adaptive stage structure).

**YAML frontmatter fields:**
```yaml
---
id: {{TASK_ID}}
created: {{CREATED_AT}}
updated: {{CREATED_AT}}
description: "{{DESCRIPTION}}"

# Stage tracking (linear by default)
stage: refinement
stages:
  refinement: in_progress
  grilling: pending
  execution: pending
  verification: pending

# Confidence calculation (per D-04, D-05)
confidence:
  score: {{CONFIDENCE_SCORE}}
  factors:
    context: {{CONTEXT_FACTOR}}
    scope: {{SCOPE_FACTOR}}
    complexity: {{COMPLEXITY_FACTOR}}
    familiarity: {{FAMILIARITY_FACTOR}}

# Wave breakdown (only added when user confirms complex task - per D-03)
# waves:
#   - name: <wave-name>
#     description: "<wave-description>"
#     status: pending
---
```

**Body sections:**
1. `# {{TITLE}}` - Task title from description
2. `## Problem Statement` - Per D-01: comprehensive for session resume. Include symptoms, impact, related context.
3. `## Context Loaded` - List of docs loaded via keyword matching
4. `## Approach` - Refine agent's analysis of how to solve
5. `## Concerns` - Potential issues or uncertainties
6. `## Execution Log` - Comment placeholder for implementation phase

Use placeholder syntax `{{PLACEHOLDER}}` consistent with existing templates (project-template.md, config-template.json).
  </action>
  <verify>
    <automated>test -f skills/do/references/task-template.md && grep -c "{{" skills/do/references/task-template.md</automated>
  </verify>
  <done>Template file exists with all required placeholders for YAML frontmatter and body sections</done>
</task>

</tasks>

<verification>
1. load-task-context.cjs runs without error: `node skills/do/scripts/load-task-context.cjs --help`
2. Keyword extraction works: Test with sample descriptions
3. task-template.md has all required placeholders for frontmatter and body
4. Both files follow existing patterns in the codebase
</verification>

<success_criteria>
- load-task-context.cjs extracts keywords and finds matching docs
- task-template.md defines complete task markdown structure
- Both assets ready for use by /do:task skill implementation
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-creation-refine-agent/05-01-SUMMARY.md`
</output>
