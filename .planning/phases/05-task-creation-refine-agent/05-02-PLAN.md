---
phase: 05-task-creation-refine-agent
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - skills/do/SKILL.md
autonomous: true
requirements:
  - TS-06

must_haves:
  truths:
    - "User can run /do:task 'description' to create a task file"
    - "Active task blocks new task creation with continue/abandon options"
    - "Task file is created in .do/tasks/ with correct filename format"
    - "Confidence score is calculated and displayed with factor breakdown"
    - "User is ALWAYS asked about wave breakdown regardless of complexity"
    - "auto_grill_threshold is read from config.json for threshold comparison"
    - "config.json active_task is updated when task is created"
  artifacts:
    - path: "skills/do/SKILL.md"
      provides: "/do:task command implementation"
      contains: "## /do:task"
    - path: ".do/tasks/*.md"
      provides: "Task markdown file (created at runtime)"
      contains: "stage: refinement"
  key_links:
    - from: "skills/do/SKILL.md /do:task"
      to: "skills/do/scripts/check-database-entry.cjs"
      via: "prerequisite gate check"
      pattern: "check-database-entry.cjs"
    - from: "skills/do/SKILL.md /do:task"
      to: "skills/do/scripts/load-task-context.cjs"
      via: "context loading step"
      pattern: "load-task-context.cjs"
    - from: "skills/do/SKILL.md /do:task"
      to: "skills/do/references/task-template.md"
      via: "template reference"
      pattern: "@skills/do/references/task-template.md"
    - from: "skills/do/SKILL.md /do:task"
      to: ".do/config.json"
      via: "active_task read/write"
      pattern: "active_task"
---

<objective>
Implement the /do:task command in SKILL.md that spawns the refine agent to analyze tasks, load context, and create task markdown files.

Purpose: This is the main entry point for task creation. It orchestrates the full refinement flow per CONTEXT.md decisions D-01 through D-12.

Output: Working /do:task command that creates documented task files with confidence scoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-task-creation-refine-agent/05-CONTEXT.md
@.planning/phases/05-task-creation-refine-agent/05-RESEARCH.md

# Prior plan artifacts
@.planning/phases/05-task-creation-refine-agent/05-01-SUMMARY.md

# Current skill file to extend
@skills/do/SKILL.md

# Scripts and templates to reference
@skills/do/scripts/load-task-context.cjs
@skills/do/scripts/check-database-entry.cjs
@skills/do/references/task-template.md
@skills/do/references/config-template.json
</context>

<interfaces>
<!-- Key contracts from Plan 01 artifacts -->

From load-task-context.cjs (created in Plan 01):
```javascript
// CLI output format
{
  "project_md_path": "/path/to/database/projects/my-project/project.md",
  "matched_docs": ["/path/to/components/FormFields.md", ...],
  "keywords": ["form", "validation", "error"],
  "database_path": "/path/to/database/projects/my-project"
}
```

From config-template.json:
```json
{
  "active_task": null,          // Set to filename when task created
  "auto_grill_threshold": 0.9   // Confidence threshold for grill-me
}
```

From task-template.md (created in Plan 01):
```yaml
---
id: {{TASK_ID}}
stage: refinement
confidence:
  score: {{CONFIDENCE_SCORE}}
  factors:
    context: {{CONTEXT_FACTOR}}
    scope: {{SCOPE_FACTOR}}
    complexity: {{COMPLEXITY_FACTOR}}
    familiarity: {{FAMILIARITY_FACTOR}}
---
# {{TITLE}}
## Problem Statement
{{PROBLEM_STATEMENT}}
...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace /do:task placeholder with full implementation</name>
  <files>skills/do/SKILL.md</files>
  <action>
**IMPORTANT: Use /skill-creator to modify SKILL.md per project CLAUDE.md requirement.**

Replace the existing `/do:task (Planned - Phase 5)` placeholder section with the full implementation.

The /do:task section should implement the refine agent as inline prompt logic (not a separate agent file), following the established Phase 2-4 interactive pattern.

**Structure:**

```markdown
## /do:task

Create and refine a task with AI assistance.

### Usage

```
/do:task "description of what you want to accomplish"
```

### Prerequisites

[Keep existing prerequisite checks and database entry gate logic]

### Refinement Process

**Step 1: Check prerequisites**

Run database entry check:
```bash
node <skill-path>/scripts/check-database-entry.cjs --message
```

If exit code is non-zero, display error and stop.

**Step 2: Check for active task (per D-11)**

Read `.do/config.json`. If `active_task` is not null:

1. Check if task file exists: `.do/tasks/<active_task>`
2. If exists, read YAML frontmatter to get stage
3. Display blocking message:

```
Active task: <active_task> (stage: <stage>)

Options:
- /do:continue - Resume this task
- /do:abandon - Mark as abandoned and start fresh
```

Wait for user response. Do not proceed until resolved.

**Step 3: Load context (per D-07, D-08, D-09, D-10)**

Run context loading script:
```bash
node <skill-path>/scripts/load-task-context.cjs "<task-description>"
```

Parse JSON output for:
- `project_md_path` - Path to project.md
- `matched_docs` - Array of matched component/tech/feature docs
- `keywords` - Extracted keywords for transparency
- `database_path` - Path to project's database folder

Read project.md and each matched doc to gather context.

**Step 4: Analyze task**

With loaded context, analyze the task description:
- Identify what systems/components it touches
- Identify potential implementation approach
- Identify concerns or uncertainties
- Check if similar patterns exist in codebase

**Step 5: Calculate confidence (per D-04, D-05)**

Starting from 1.0, apply deductions:

| Factor | Deduction | Condition |
|--------|-----------|-----------|
| context | -0.1 to -0.2 | Missing component/tech docs that task likely needs |
| scope | -0.05 to -0.15 | Task spans multiple files/systems |
| complexity | -0.05 to -0.15 | Multiple integration points |
| familiarity | -0.05 to -0.1 | No similar patterns found in codebase |

Display breakdown transparently (per D-05):
```
Confidence: 0.72 (context: -0.10, scope: -0.10, complexity: -0.08, familiarity: 0.00)
```

**Step 5.5: Read grill threshold from config (per D-06)**

Read `.do/config.json` and extract `auto_grill_threshold` (default 0.9 if not set).
Store for use in Step 9 to determine if grill-me will trigger.

**Step 6: Propose wave breakdown (per D-02, D-03)**

**ALWAYS ask the user about wave breakdown** — per D-03, refine agent always asks, user decides:

```
This task {complexity assessment - e.g., "touches 3 systems" or "is focused on one component"}.

Break into waves, or execute as single unit?

1. Yes - Document waves before proceeding
2. No - Execute as single unit

Enter choice (default: 2):
```

Wait for user response.

If user selects "1" (waves):
```
Enter wave names and descriptions (one per line, format: name: description)
Enter a blank line when done:

Example:
api-types: Generate upload endpoint types
upload-hook: Create useDocumentUpload hook
ui-components: Build UploadDropzone and Preview
```

Collect wave definitions from user.

**Step 7: Create task file**

Generate filename: `YYMMDD-<slug>.md`
- Extract first 5 words from description
- Kebab-case, remove special characters
- Example: "Fix login validation errors" -> `260413-fix-login-validation-errors.md`

Read task-template.md:
@skills/do/references/task-template.md

Replace placeholders:
- `{{TASK_ID}}` - filename without .md
- `{{CREATED_AT}}` - ISO 8601 timestamp
- `{{DESCRIPTION}}` - user's task description
- `{{CONFIDENCE_SCORE}}` - calculated score
- `{{CONTEXT_FACTOR}}` - context deduction
- `{{SCOPE_FACTOR}}` - scope deduction
- `{{COMPLEXITY_FACTOR}}` - complexity deduction
- `{{FAMILIARITY_FACTOR}}` - familiarity deduction
- `{{TITLE}}` - task description as title (first ~50 chars)
- `{{PROBLEM_STATEMENT}}` - comprehensive problem statement (generate from analysis)
- `{{CONTEXT_LOADED}}` - list of loaded docs
- `{{APPROACH}}` - planned implementation approach
- `{{CONCERNS}}` - identified concerns

If user confirmed wave breakdown, add waves section to frontmatter:
```yaml
waves:
  - name: <wave-name>
    description: "<wave-description>"
    status: pending
```

Write task file to `.do/tasks/<filename>`

**Step 8: Update config.json**

Read `.do/config.json`
Set `active_task` to the task filename
Write back to `.do/config.json`

**Step 9: Display summary**

```
Task created: .do/tasks/<filename>

Confidence: <score> (<factor breakdown>)
{If below threshold:}
-> Grill-me phase will ask clarifying questions

Context loaded:
- project.md
{For each matched doc:}
- <doc-name>

Problem: <1-line summary>
Approach: <1-line summary>
{If waves:}
Waves: <count> defined

Next: {If confidence >= auto_grill_threshold: "Ready for implementation. Run /do:continue when ready (Phase 7)."}
      {If confidence < auto_grill_threshold: "Grill-me will ask clarifying questions. Run /do:continue (Phase 6)."}
```

### /do:abandon

Mark the active task as abandoned and allow starting a new task.

**Usage:**
```
/do:abandon
```

**Process:**

1. Read `.do/config.json` to get `active_task`
2. If no active task, display: "No active task to abandon."
3. Read task file at `.do/tasks/<active_task>`
4. Update YAML frontmatter: `stage: abandoned`
5. Write back to task file
6. Update config.json: `active_task: null`
7. Display: "Task abandoned: <filename>. You can now start a new task with /do:task."

### Files

- **Context loading script:**
  - @skills/do/scripts/load-task-context.cjs

- **Task template:**
  - @skills/do/references/task-template.md

- **Gate script:**
  - @skills/do/scripts/check-database-entry.cjs
```

**Key implementation notes:**
- Use inline prompts (not AskUserQuestion) per workspace CLAUDE.md bug documentation
- Wait for user response after each prompt that requires input
- Confidence calculation happens during analysis, displayed transparently
- Task file format per D-01, D-02 from CONTEXT.md
- Active task blocking per D-11, abandon per D-12
  </action>
  <verify>
    <automated>grep -c "## /do:task" skills/do/SKILL.md && grep -c "## /do:abandon" skills/do/SKILL.md && grep -c "load-task-context.cjs" skills/do/SKILL.md</automated>
  </verify>
  <done>/do:task section fully implemented with all 9 steps, /do:abandon section added, references to load-task-context.cjs and task-template.md included</done>
</task>

<task type="auto">
  <name>Task 2: Update Quick Reference table</name>
  <files>skills/do/SKILL.md</files>
  <action>
**Use /skill-creator to modify SKILL.md.**

Update the Quick Reference table at the top of SKILL.md to reflect the current command status:

```markdown
## Quick Reference

| Command | Purpose |
|---------|---------|
| `/do:init` | Initialize workspace or run health check |
| `/do:scan` | Scan project and create database entry |
| `/do:task` | Create and refine a task |
| `/do:abandon` | Abandon active task |
| `/do:continue` | Resume from last task state (Phase 9) |
| `/do:debug` | Structured debugging workflow (Phase 10) |
```

Also update the "Planned Commands" section to remove /do:task and /do:abandon (now implemented), keeping only:
- `/do:continue` - Resume from last task state
- `/do:debug` - Structured debugging workflow
  </action>
  <verify>
    <automated>grep -A10 "## Quick Reference" skills/do/SKILL.md | grep -c "abandon"</automated>
  </verify>
  <done>Quick Reference table updated with /do:task and /do:abandon, Planned Commands section updated</done>
</task>

</tasks>

<verification>
1. SKILL.md has full /do:task implementation: `grep "## /do:task" skills/do/SKILL.md`
2. SKILL.md has /do:abandon section: `grep "## /do:abandon" skills/do/SKILL.md`
3. Context loading is referenced: `grep "load-task-context.cjs" skills/do/SKILL.md`
4. Task template is referenced: `grep "task-template.md" skills/do/SKILL.md`
5. Confidence factors are documented: `grep -A5 "Factor.*Deduction" skills/do/SKILL.md`
6. Active task blocking is implemented: `grep -A5 "active_task" skills/do/SKILL.md`
</verification>

<success_criteria>
- /do:task command fully documented with all 9 refinement steps
- /do:abandon command documented
- All decisions D-01 through D-12 from CONTEXT.md addressed
- Script and template references correct
- Quick Reference and Planned Commands sections updated
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-creation-refine-agent/05-02-SUMMARY.md`
</output>
