---
phase: 06-grill-me-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/do/references/task-template.md
  - skills/do/SKILL.md
autonomous: true
requirements: [TS-07]

must_haves:
  truths:
    - "Confidence < threshold triggers grill-me flow"
    - "Grill-me asks targeted questions about weakest confidence factor"
    - "User answers update Clarifications section in task markdown"
    - "Confidence recalculates after each answer"
    - "User can override with 'Proceed anyway' at any point"
    - "stages.grilling is set to in_progress when entering grill flow"
    - "stages.grilling is set to complete when exiting grill flow"
    - "Stale task file shows recovery message and clears active_task"
  artifacts:
    - path: "skills/do/references/task-template.md"
      provides: "Clarifications section placeholder"
      contains: "## Clarifications"
    - path: "skills/do/SKILL.md"
      provides: "/do:continue with grill-me flow"
      contains: "## /do:continue"
  key_links:
    - from: "skills/do/SKILL.md"
      to: ".do/config.json"
      via: "auto_grill_threshold read"
      pattern: "auto_grill_threshold"
    - from: "skills/do/SKILL.md"
      to: ".do/tasks/<active_task>"
      via: "confidence.factors read and Clarifications write"
      pattern: "confidence\\.factors"
    - from: "skills/do/SKILL.md"
      to: ".do/tasks/<active_task>"
      via: "stages.grilling state updates"
      pattern: "stages\\.grilling"
---

<objective>
Implement grill-me logic within `/do:continue` that triggers when a task's confidence is below the configured threshold, asking targeted questions about the weakest factor until confidence meets threshold or user overrides.

Purpose: Force clarification on low-confidence tasks before implementation, reducing wasted execution cycles.
Output: Extended SKILL.md with `/do:continue` command and grill-me flow, updated task-template.md with Clarifications section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-grill-me-agent/06-CONTEXT.md
@.planning/phases/06-grill-me-agent/06-RESEARCH.md
@.planning/phases/05-task-creation-refine-agent/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From skills/do/references/config-template.json:
```json
{
  "version": "0.1.0",
  "project_name": "{{PROJECT_NAME}}",
  "database_entry": false,
  "active_task": null,
  "auto_grill_threshold": 0.9,
  "council_reviews": { ... }
}
```

From skills/do/references/task-template.md YAML frontmatter:
```yaml
stage: refinement  # Valid: refinement, execution, verification, abandoned
stages:
  refinement: in_progress
  grilling: pending       # in_progress during grill, complete when done
  execution: pending
  verification: pending
  abandoned: false

confidence:
  score: 0.72  # 0.0-1.0
  factors:
    context: -0.10    # Deduction from 1.0
    scope: -0.08
    complexity: -0.05
    familiarity: -0.05
```

Factor priority (for tie-breaking): context > scope > complexity > familiarity

**IMPORTANT:** `grilling` is NOT a top-level stage value. It is tracked via `stages.grilling: pending|in_progress|complete`. The top-level `stage` remains `refinement` throughout the grill-me flow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Clarifications section to task-template.md</name>
  <files>skills/do/references/task-template.md</files>
  <read_first>
    - skills/do/references/task-template.md (current template to modify)
    - .planning/phases/06-grill-me-agent/06-CONTEXT.md (D-16 specifies exact format)
  </read_first>
  <action>
Add a `## Clarifications` section to task-template.md BETWEEN `## Problem Statement` and `## Context Loaded` (per D-16).

The section should contain:
1. A heading `## Clarifications`
2. A placeholder comment explaining the format
3. No actual content (populated by grill-me flow)

Insert this exact content after the `## Problem Statement` section:

```markdown
## Clarifications

<!--
Populated by grill-me flow when confidence < threshold.
Format:

### <Factor> (was: <old_value> -> now: <new_value>)
**Q:** <question asked>
**A:** <user's answer>

If user overrides before reaching threshold:
"User override at confidence <score>"
-->
```

This placement is critical - Clarifications must come AFTER Problem Statement (which establishes what the task is) but BEFORE Context Loaded (which documents what was found). The grill-me flow adds Q&A pairs to this section as the user answers questions.
  </action>
  <verify>
    <automated>grep -A 3 "## Clarifications" skills/do/references/task-template.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Clarifications" skills/do/references/task-template.md returns match
    - grep "was:.*->.*now:" skills/do/references/task-template.md returns match (format example in comment)
    - Section appears after "## Problem Statement" and before "## Context Loaded" in file order
  </acceptance_criteria>
  <done>task-template.md has Clarifications section placeholder in correct position with format documentation</done>
</task>

<task type="auto">
  <name>Task 2: Implement /do:continue with grill-me flow in SKILL.md</name>
  <files>skills/do/SKILL.md</files>
  <read_first>
    - skills/do/SKILL.md (current skill file - see existing /do:task and /do:abandon patterns, especially lines 718-726 for stale task handling)
    - .planning/phases/06-grill-me-agent/06-CONTEXT.md (D-13 through D-17)
    - .planning/phases/06-grill-me-agent/06-RESEARCH.md (Pattern 1: Grill-Me Flow, Pattern 2: Clarifications Format)
    - .planning/phases/06-grill-me-agent/06-01-REVIEWS.md (council feedback on stage model and stale task handling)
    - skills/do/references/task-template.md (task markdown structure)
  </read_first>
  <action>
Add a complete `/do:continue` section to SKILL.md following the existing `/do:abandon` section. This implements the grill-me flow per decisions D-13 through D-17, WITH council-mandated fixes for stage model and stale task handling.

**COUNCIL FIXES REQUIRED:**
1. Remove `grilling` as a routable top-level stage (it's NOT a separate stage)
2. Add routing for `stage: refinement` + `stages.grilling: in_progress` -> resume grill-me flow
3. Add stale task file check in Step 1 (follow /do:task pattern from lines 718-726)
4. Set `stages.grilling: in_progress` when entering grill flow
5. Set `stages.grilling: complete` when exiting grill flow

**Section structure:**

```markdown
## /do:continue

Resume the active task from its current state.

### Stage Detection

**Step 1: Load active task**

Read `.do/config.json` to get `active_task`.
If no active task, display: "No active task. Run /do:task to create one."

Check if task file exists at `.do/tasks/<active_task>`:
- If file does NOT exist (stale pointer):
  - Display: "Warning: active_task points to missing file '<active_task>'. Clearing stale reference."
  - Update config.json: set `active_task: null`
  - Display: "No active task. Run /do:task to create one."
  - Stop

Read task file at `.do/tasks/<active_task>`.
Parse YAML frontmatter for `stage`, `stages`, and `confidence`.

**Step 2: Route by stage**

| Stage | Condition | Route |
|-------|-----------|-------|
| refinement | stages.grilling: in_progress | Resume grill-me flow (Step G1) |
| refinement | confidence < threshold | Start grill-me flow (Step G0) |
| refinement | confidence >= threshold | Display: "Ready for implementation. (Phase 7 - not yet implemented)" |
| execution | any | Display: "Execution not yet implemented. (Phase 7)" |
| verification | any | Display: "Verification not yet implemented. (Phase 8)" |
| abandoned | any | Display: "Task was abandoned. Run /do:task to create a new one." |

Read `auto_grill_threshold` from `.do/config.json` (default 0.9 if not set).

**NOTE:** `grilling` is NOT a valid top-level stage value. Grill status is tracked via `stages.grilling` field (pending/in_progress/complete).

### Grill-Me Flow

Run this flow when stage is `refinement` AND (confidence.score < auto_grill_threshold OR stages.grilling: in_progress).

**Step G0: Enter grill-me flow (first time only)**

If `stages.grilling` is `pending`:
- Update task frontmatter: `stages.grilling: in_progress`
- Update `updated` timestamp
- Continue to Step G1

**Step G1: Identify weakest factor (per D-13)**

Read `confidence.factors` from task frontmatter.
Find factor with largest deduction (most negative value).
If multiple tied, use priority order: context > scope > complexity > familiarity.

**Step G2: Generate targeted question (per D-14)**

Based on the weakest factor, generate a specific question. Use inline text prompt (NOT AskUserQuestion - documented bug).

| Factor | Question Pattern |
|--------|------------------|
| context | "What existing component/pattern should this use? Any specific files or docs I should reference?" |
| scope | "Which specific files/functions need modification? Are there any areas explicitly OUT of scope?" |
| complexity | "How do these systems interact? Any dependencies or edge cases I should know about?" |
| familiarity | "Has similar work been done before in this codebase? Any reference implementations to follow?" |

Display to user:
```
Confidence: <score> (threshold: <threshold>)
Weakest factor: <factor> (<value>)

<targeted question>

Enter your answer, or type "Proceed anyway" to skip remaining questions:
```

**Step G3: Process user response (per D-15)**

If user types "Proceed anyway" (case-insensitive):
- Add to Clarifications section: `User override at confidence <score>`
- Update task frontmatter:
  - `stages.grilling: complete`
  - `stages.refinement: complete`
- Update `updated` timestamp
- Display: "Proceeding with confidence <score>. Ready for implementation. (Phase 7 - not yet implemented)"
- Stop grill-me flow

Otherwise, process the answer.

**Step G4: Update task markdown (per D-16)**

Add to Clarifications section:

```markdown
### <Factor> (was: <old_value> -> now: <new_value>)
**Q:** <question asked>
**A:** <user's answer>
```

Calculate confidence boost based on answer specificity:
- Contains file path (e.g., `src/`, `.ts`, `.md`): +0.03
- Contains PascalCase component name: +0.02
- Contains specific term (src, lib, utils, hooks, components, api, endpoint): +0.02
- Base boost for any non-skip answer: +0.03
- Maximum boost per answer: 0.10

Update confidence.factors.<factor> with new value (old + boost, capped at 0.00).
Recalculate confidence.score = 1.0 + sum(all factors).
Update `updated` timestamp to current ISO-8601.

**Step G5: Check threshold or loop (per D-15)**

If confidence.score >= auto_grill_threshold:
- Update task frontmatter:
  - `stages.grilling: complete`
  - `stages.refinement: complete`
- Display:
  ```
  Confidence: <old_score> -> <new_score> (+<delta>)
  <factor> improved.
  
  Confidence threshold met. Ready for implementation. (Phase 7 - not yet implemented)
  ```
- Stop grill-me flow

Else:
- Display:
  ```
  Confidence: <old_score> -> <new_score> (+<delta>)
  <factor> improved. Next weakest: <next_factor>
  ```
- Return to Step G1 with the next weakest factor

### Files

- **Config:** `.do/config.json` - Read `active_task`, `auto_grill_threshold`
- **Task:** `.do/tasks/<active_task>` - Read/write frontmatter and Clarifications section
```

**Key implementation notes:**
1. The skill markdown is INSTRUCTIONS for Claude, not executable code
2. Use inline prompts, display text, then wait for user response
3. Keep top-level `stage` as `refinement` throughout grilling (grilling is tracked via `stages.grilling` only)
4. When entering grill flow: set `stages.grilling: in_progress`
5. When exiting grill flow (threshold met OR override): set `stages.grilling: complete`, `stages.refinement: complete`
6. Stale task file handling MUST follow the pattern from /do:task lines 718-726
7. Update Quick Reference table: `/do:continue` should say "Resume from last task state"
  </action>
  <verify>
    <automated>grep -c "## /do:continue" skills/do/SKILL.md && grep -c "Grill-Me Flow" skills/do/SKILL.md && grep -c "stages.grilling" skills/do/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## /do:continue" skills/do/SKILL.md returns 1 match
    - grep "### Grill-Me Flow" skills/do/SKILL.md returns 1 match
    - grep "Step G0:" skills/do/SKILL.md returns 1 match (enter grill-me flow)
    - grep "Step G1:" skills/do/SKILL.md returns 1 match (weakest factor identification)
    - grep "Step G2:" skills/do/SKILL.md returns 1 match (targeted question)
    - grep "Step G3:" skills/do/SKILL.md returns 1 match (process response)
    - grep "Step G4:" skills/do/SKILL.md returns 1 match (update task markdown)
    - grep "Step G5:" skills/do/SKILL.md returns 1 match (threshold check)
    - grep "Proceed anyway" skills/do/SKILL.md returns match (override option per D-15)
    - grep "context > scope > complexity > familiarity" skills/do/SKILL.md returns match (priority order per D-13)
    - grep "stages.grilling: in_progress" skills/do/SKILL.md returns match (council fix: state persistence on entry)
    - grep "stages.grilling: complete" skills/do/SKILL.md returns match (council fix: state persistence on exit)
    - grep "Clearing stale reference" skills/do/SKILL.md returns match (council fix: stale task handling)
    - Route by stage table does NOT have a separate row for "grilling" as top-level stage
    - Route by stage table HAS "stages.grilling: in_progress" as a condition for resuming grill flow
    - Quick Reference shows "/do:continue" with "Resume from last task state"
  </acceptance_criteria>
  <done>/do:continue section fully implemented with grill-me flow covering all 6 steps (G0-G5), stage routing with stages.grilling conditions, stale task handling, and override handling</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Verify task-template.md has Clarifications section in correct position
2. Verify SKILL.md has /do:continue with complete grill-me flow
3. Verify all decisions D-13 through D-17 are implemented:
   - D-13: Lowest confidence factor targeted first (Step G1)
   - D-14: Inline text prompts (Step G2)
   - D-15: Stop at threshold with override option (Steps G3, G5)
   - D-16: Clarifications section format (Step G4)
   - D-17: Inline in SKILL.md, not separate agent file (structure)
4. Verify council fixes implemented:
   - Stale task handling in Step 1 (grep "Clearing stale reference")
   - stages.grilling: in_progress when entering (grep "stages.grilling: in_progress")
   - stages.grilling: complete when exiting (grep "stages.grilling: complete")
   - No "grilling" row in Route by stage table (grep -v "^\| grilling")
   - stages.grilling condition in routing table
</verification>

<success_criteria>
- task-template.md contains `## Clarifications` section between Problem Statement and Context Loaded
- SKILL.md contains `## /do:continue` section with complete grill-me flow
- Grill-me flow implements all 6 steps (G0-G5)
- Stage routing handles all valid stages (refinement, execution, verification, abandoned) with stages.grilling conditions
- Stale task file handling shows recovery message and clears active_task
- stages.grilling is set to in_progress when entering grill flow
- stages.grilling is set to complete when exiting grill flow (threshold met OR user override)
- Override option documented with "Proceed anyway"
- Quick Reference table updated for /do:continue
</success_criteria>

<output>
After completion, create `.planning/phases/06-grill-me-agent/06-01-SUMMARY.md`
</output>
