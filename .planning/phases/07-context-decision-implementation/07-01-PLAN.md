---
phase: 07-context-decision-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/do/SKILL.md
  - skills/do/references/stage-grill.md
autonomous: true
requirements: [TS-08, TS-09]

must_haves:
  truths:
    - "Grill-me flow is extracted to a reference file"
    - "/do:continue routes to stage-specific reference files"
    - "SKILL.md /do:continue section is <150 lines (routing only)"
  artifacts:
    - path: "skills/do/references/stage-grill.md"
      provides: "Grill-me flow instructions"
      contains: "Step G0"
    - path: "skills/do/SKILL.md"
      provides: "Stage router"
      contains: "@skills/do/references/stage-grill.md"
  key_links:
    - from: "skills/do/SKILL.md"
      to: "skills/do/references/stage-grill.md"
      via: "@reference syntax"
      pattern: "@skills/do/references/stage-grill.md"
---

<objective>
Refactor SKILL.md `/do:continue` into a stage router and extract grill-me flow to a reference file.

Purpose: Enable conditional loading of stage-specific instructions (D-19 architectural foundation). This is required before Plan 02 can add execution flow.

Output:
- `skills/do/references/stage-grill.md` containing Steps G0-G5
- `skills/do/SKILL.md` with routing table that loads reference files by stage
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-context-decision-implementation/07-CONTEXT.md
@.planning/phases/07-context-decision-implementation/07-RESEARCH.md

<interfaces>
<!-- Key patterns from existing implementation that executor needs -->

From skills/do/SKILL.md (current /do:continue routing table at line 938-947):
```markdown
| Stage | Condition | Route |
|-------|-----------|-------|
| refinement | stages.grilling: complete | Display: "Ready for implementation. (Phase 7 - not yet implemented)" |
| refinement | stages.grilling: in_progress | Resume grill-me flow (Step G1) |
| refinement | confidence < threshold | Start grill-me flow (Step G0) |
| refinement | confidence >= threshold | Display: "Ready for implementation. (Phase 7 - not yet implemented)" |
| execution | any | Display: "Execution not yet implemented. (Phase 7)" |
| verification | any | Display: "Verification not yet implemented. (Phase 8)" |
| abandoned | any | Display: "Task was abandoned. Run /do:task to create a new one." |
```

From skills/do/SKILL.md (grill-me flow at lines 951-1049):
Steps G0-G5 with:
- G0: Enter grill-me flow, update stages.grilling to in_progress
- G1: Identify weakest factor
- G2: Generate targeted question (inline prompt, NOT AskUserQuestion)
- G3: Process user response (handle "Proceed anyway")
- G4: Update task markdown with Q&A, calculate confidence boost
- G5: Check threshold or loop

Reference loading pattern (from 07-RESEARCH.md):
```markdown
| Stage | Condition | Action |
|-------|-----------|--------|
| refinement | stages.grilling: in_progress/pending AND confidence < threshold | @skills/do/references/stage-grill.md |
| refinement | stages.grilling: complete OR confidence >= threshold | Display: "Execution stage not yet available. (Plan 02 adds this)" |
| execution | any | Display: "Execution stage not yet available. (Plan 02 adds this)" |
| verification | any | Display: "Verification not yet implemented. (Phase 8)" |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stage-grill.md reference file</name>
  <files>skills/do/references/stage-grill.md</files>
  <read_first>
    - skills/do/SKILL.md (lines 951-1049 for grill-me flow Steps G0-G5)
    - skills/do/references/task-template.md (Clarifications section format)
  </read_first>
  <action>
Create `skills/do/references/stage-grill.md` by extracting the grill-me flow from SKILL.md.

1. Add YAML frontmatter:
```yaml
---
name: stage-grill
description: Grill-me flow for confidence improvement. Loaded when stage is refinement and confidence < threshold.
---
```

2. Add intro header:
```markdown
# Grill-Me Stage

This reference file is loaded by /do:continue when the task needs confidence improvement.

**Prerequisites:**
- Active task exists in `.do/tasks/`
- Task stage is `refinement`
- Task confidence.score < auto_grill_threshold from config
```

3. Extract Steps G0-G5 exactly as they appear in SKILL.md lines 955-1044. Preserve:
   - All step headings (Step G0, G1, etc.)
   - All logic for factor identification, question patterns, confidence boost calculations
   - The "Proceed anyway" override handling
   - The inline prompt format (NOT AskUserQuestion)
   - All display messages and formatting

4. At the end, add Files section:
```markdown
### Files

- **Config:** `.do/config.json` - Read `auto_grill_threshold`
- **Task:** `.do/tasks/<active_task>` - Read/write frontmatter and Clarifications section
```

The file should be ~100-120 lines (same size as extracted content plus frontmatter and intro).
  </action>
  <verify>
    <automated>grep -c "Step G" skills/do/references/stage-grill.md | grep -q "6" && echo "PASS: All 6 G-steps present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Step G0" skills/do/references/stage-grill.md` returns 0
    - `grep -q "Step G5" skills/do/references/stage-grill.md` returns 0
    - `grep -q "Proceed anyway" skills/do/references/stage-grill.md` returns 0
    - `grep -q "confidence boost" skills/do/references/stage-grill.md` returns 0
    - `grep -q "inline text prompt" skills/do/references/stage-grill.md` returns 0 (or references inline prompts)
    - File has YAML frontmatter with name and description
  </acceptance_criteria>
  <done>
    - stage-grill.md exists with Steps G0-G5
    - All grill-me logic preserved (factor targeting, confidence boost, user override)
    - Inline prompt pattern maintained (no AskUserQuestion)
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor SKILL.md /do:continue into router</name>
  <files>skills/do/SKILL.md</files>
  <read_first>
    - skills/do/SKILL.md (full file, especially /do:continue section lines 914-1050)
    - skills/do/references/stage-grill.md (created in Task 1)
  </read_first>
  <action>
Modify `skills/do/SKILL.md` to replace inline grill-me flow with routing to reference files.

1. Keep the entire `/do:continue` section up to and including the routing table (lines 914-947).

2. After the routing table, replace Steps G0-G5 (lines 951-1049) with a reference loading instruction:

```markdown
### Stage Reference Loading

Based on the routing table above, load and follow the appropriate reference file:

**For grill-me (refinement with confidence < threshold or grilling in_progress):**
@skills/do/references/stage-grill.md

**For execution (refinement ready or execution stage):**
Display: "Execution stage not yet available. Run Plan 02 first." (stub until Plan 02 creates stage-execute.md)

**For verification:**
Display: "Verification not yet implemented. (Phase 8)"

Follow the instructions in the loaded reference file to complete the stage.
```

3. Update the routing table to show which reference file handles each case:

```markdown
| Stage | Condition | Reference File |
|-------|-----------|----------------|
| refinement | stages.grilling: in_progress | @skills/do/references/stage-grill.md |
| refinement | stages.grilling: pending AND confidence < threshold | @skills/do/references/stage-grill.md |
| refinement | stages.grilling: complete OR confidence >= threshold | Display: "Execution stage not yet available." (Plan 02 adds stage-execute.md) |
| execution | any | Display: "Execution stage not yet available." (Plan 02 adds stage-execute.md) |
| verification | any | Display: "Verification not yet implemented. (Phase 8)" |
| abandoned | any | Display: "Task was abandoned. Run /do:task to create a new one." |
```

**NOTE:** Do NOT reference `@skills/do/references/stage-execute.md` yet - that file will be created in Plan 02. Do NOT reference `@skills/do/references/stage-verify.md` yet - that file will be created in Phase 8. Use display-only messages for these cases to avoid broken intermediate state.

4. Remove the old ### Grill-Me Flow section and ### Files subsection that was at the end (since that info is now in stage-grill.md).

5. Verify the /do:continue section is now ~60-80 lines (routing only, no inline grill-me logic).

Per D-19: SKILL.md becomes a router. Stage-specific logic lives in reference files.
  </action>
  <verify>
    <automated>grep -c "@skills/do/references/stage-grill.md" skills/do/SKILL.md | grep -qE "^[1-9]" && echo "PASS: Reference to stage-grill.md found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@skills/do/references/stage-grill.md" skills/do/SKILL.md` returns 0
    - `grep -q "Execution stage not yet available" skills/do/SKILL.md` returns 0 (placeholder for Plan 02)
    - `grep -q "Verification not yet implemented" skills/do/SKILL.md` returns 0 (placeholder for Phase 8)
    - `grep -q "Step G0" skills/do/SKILL.md` returns 1 (NOT present - moved to reference file)
    - `wc -l skills/do/SKILL.md` shows file is shorter (grill-me extracted)
    - Routing table includes Reference File column
    - NO reference to `@skills/do/references/stage-execute.md` (created in Plan 02)
    - NO reference to `@skills/do/references/stage-verify.md` (created in Phase 8)
  </acceptance_criteria>
  <done>
    - /do:continue is a stage router (no inline grill-me steps)
    - Routing table shows which reference file handles each case
    - Execution and verification use display-only placeholders (not broken @references)
    - SKILL.md /do:continue section is ~60-80 lines
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Grill-me preserved:** `grep -q "Step G5" skills/do/references/stage-grill.md` (all steps in reference file)
2. **Router pattern:** `grep -c "@skills/do/references/stage-grill.md" skills/do/SKILL.md` returns 1+ (reference to grill file)
3. **No duplication:** `grep -q "Step G0" skills/do/SKILL.md` returns 1 (NOT found in SKILL.md)
4. **No broken references:** `grep -q "@skills/do/references/stage-execute.md" skills/do/SKILL.md` returns 1 (NOT found - Plan 02 adds this)
5. **No broken references:** `grep -q "@skills/do/references/stage-verify.md" skills/do/SKILL.md` returns 1 (NOT found - Phase 8 adds this)
6. **File count:** `ls skills/do/references/stage-*.md | wc -l` returns 1 (only stage-grill.md exists after Plan 01)
</verification>

<success_criteria>
- stage-grill.md contains complete grill-me flow (Steps G0-G5)
- SKILL.md /do:continue routes to reference files instead of inline logic
- Routing table updated with Reference File column per D-19
- No duplicate grill-me logic (all in reference file, none in SKILL.md)
- Execution and verification stages use display-only placeholders (not @references to non-existent files)
</success_criteria>

<output>
After completion, create `.planning/phases/07-context-decision-implementation/07-01-SUMMARY.md`
</output>
