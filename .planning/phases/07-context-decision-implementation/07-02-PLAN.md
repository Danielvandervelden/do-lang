---
phase: 07-context-decision-implementation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - skills/do/references/stage-execute.md
  - skills/do/references/task-template.md
  - skills/do/SKILL.md
autonomous: true
requirements: [TS-08, TS-09]

must_haves:
  truths:
    - "User is prompted about context clearing before implementation"
    - "Execution logs files changed and decisions made"
    - "Stage transitions correctly after execution"
    - "Deviations from plan require user confirmation"
  artifacts:
    - path: "skills/do/references/stage-execute.md"
      provides: "Execution flow with context clear and logging"
      contains: "Step E0"
    - path: "skills/do/references/task-template.md"
      provides: "Updated Execution Log format per D-20"
      contains: "YYYY-MM-DD HH:MM"
  key_links:
    - from: "skills/do/SKILL.md"
      to: "skills/do/references/stage-execute.md"
      via: "@reference syntax"
      pattern: "@skills/do/references/stage-execute.md"
    - from: "skills/do/references/stage-execute.md"
      to: ".do/tasks/<task>.md"
      via: "Execution Log section append"
      pattern: "## Execution Log"
---

<objective>
Create the execution stage reference file with context clear decision and implementation flow.

Purpose: Implement TS-08 (context clear prompt) and TS-09 (task execution with logging). User is always prompted about clearing context. Execution follows documented approach with deviation handling.

Output:
- `skills/do/references/stage-execute.md` with Steps E0-E3
- Updated `skills/do/references/task-template.md` with D-20 execution log format
- Updated `skills/do/SKILL.md` routing table to reference stage-execute.md
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-context-decision-implementation/07-CONTEXT.md
@.planning/phases/07-context-decision-implementation/07-RESEARCH.md

<interfaces>
<!-- Decisions and patterns from 07-CONTEXT.md that executor MUST implement -->

D-18 (Hybrid context clear prompt):
```markdown
Try AskUserQuestion first ("Clear context before implementation?"), if empty/fails, 
fall back to inline text prompt. Log which method succeeded.
```

D-20 (Execution log format):
```markdown
## Execution Log

### YYYY-MM-DD HH:MM
**Files:**
- `path/to/file.ts` - Change summary

**Decisions:**
- Plan said X - chose approach Y because Z
- [If error] Tried A, failed because B, resolved with C

**Status:** In progress / Execution complete
```

D-21 (Deviation handling):
```markdown
Stop and ask user on ANY deviation from plan. No autonomous resolution. Format:
Plan said: [original instruction]
Issue: [what happened]

Options:
1. [Alternative A]
2. [Alternative B]
3. Pause and investigate
```

D-22 (Stage transitions after execution):
```markdown
- Update `stage: verification`
- Update `stages.execution: complete`
- Update `stages.verification: in_progress`
- Display: "Execution complete. Proceeding to verification. (Phase 8 - not yet implemented)"
```

From task-template.md (current Execution Log placeholder at lines 95-106):
```markdown
## Execution Log

<!--
This section is populated during the implementation phase.
Each entry should include: timestamp, action taken, result.

Format:
### YYYY-MM-DD HH:MM
- Action: <what was done>
- Result: <outcome>
- Files: <files modified>
-->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stage-execute.md reference file</name>
  <files>skills/do/references/stage-execute.md</files>
  <read_first>
    - skills/do/SKILL.md (routing table and /do:continue structure)
    - skills/do/references/stage-grill.md (reference file pattern established in Plan 01)
    - skills/do/references/task-template.md (Approach, Concerns, Execution Log sections)
    - .planning/phases/07-context-decision-implementation/07-CONTEXT.md (D-18 through D-22)
  </read_first>
  <action>
Create `skills/do/references/stage-execute.md` with the complete execution flow.

1. Add YAML frontmatter:
```yaml
---
name: stage-execute
description: Implementation execution flow. Handles context clear decision and task execution with logging.
---
```

2. Add intro section:
```markdown
# Execution Stage

This reference file is loaded by /do:continue when the task is ready for implementation.

**Prerequisites:**
- Active task exists in `.do/tasks/`
- Task stage is `refinement` with `stages.grilling: complete` OR confidence >= threshold
- OR task stage is `execution` (resuming)

**Entry conditions:**
- If stage is `refinement`, first transition to `execution` stage
- If stage is already `execution`, resume from current state
```

3. Add Step E0: Context Clear Decision (per D-18)
```markdown
### Step E0: Context Clear Decision (per D-18)

**First entry only (stage was refinement):**

Before starting execution, ask user about clearing context.

Try AskUserQuestion:
```javascript
AskUserQuestion({
  header: "Context",
  question: "Clear context before implementation?",
  options: [
    { label: "Yes", description: "Run /clear, then /do:continue to resume with fresh context" },
    { label: "No", description: "Proceed with current context" }
  ],
  multiSelect: false
})
```

**If response is empty, undefined, or tool fails:**
Fall back to inline text prompt:
```
Context Decision

Before implementing, you can clear context to reduce token usage.

Options:
1. Yes - Clear context (run /clear, then /do:continue)
2. No - Proceed with current context

Enter 1 or 2 (default: 2):
```

**Process response:**
- If "Yes" or "1": Update task stage, display instructions, STOP
  - Update frontmatter: `stage: execution`, `stages.refinement: complete`, `stages.execution: pending`
  - Display: "Run /clear to clear context, then /do:continue to resume execution."
  - Do NOT proceed with execution yet
- If "No" or "2" or empty (default): Continue to Step E1
  - Update frontmatter: `stage: execution`, `stages.refinement: complete`, `stages.execution: in_progress`

**Log context decision:**
Add to Execution Log:
```markdown
### <timestamp>
**Context decision:** [AskUserQuestion|inline prompt] - user chose [Yes|No]
```

**If resuming (stage was already execution):** Skip E0, continue to E1.
```

4. Add Step E1: Load task context
```markdown
### Step E1: Load Task Context

Read task markdown for:
- Problem Statement (what to solve)
- Approach (how to solve it)
- Concerns (what to watch for)
- Context Loaded (relevant docs)
- Clarifications (from grill-me, if any)

Load each file from Context Loaded section to understand the codebase patterns.
```

5. Add Step E2: Execute implementation
```markdown
### Step E2: Execute Implementation

Follow the Approach section step by step.

**Execution rules:**
1. Execute changes following the documented approach
2. After each significant action, update the Execution Log (per D-20)
3. If plan says X but you encounter Y, STOP and ask (per D-21)

**Log entry format (per D-20):**
```markdown
### <YYYY-MM-DD HH:MM>
**Files:**
- `path/to/file.ts` - Change summary

**Decisions:**
- Plan said X - chose approach Y because Z
- [If error] Tried A, failed because B, resolved with C

**Status:** In progress
```

**Deviation handling (per D-21):**
If ANY deviation from plan:
```
Plan said: [original instruction from Approach section]
Issue: [what actually happened or what's different]

Options:
1. [Alternative A]
2. [Alternative B]
3. Pause and investigate

Which option?
```
Wait for user response. Log the decision in Execution Log with "User chose: X".
```

6. Add Step E3: Update task state
```markdown
### Step E3: Update Task State After Execution

After all execution is complete:

1. Update frontmatter:
   - `stage: verification`
   - `stages.execution: complete`
   - `stages.verification: in_progress`
   - `updated: <ISO-8601 timestamp>`

2. Final log entry:
```markdown
### <timestamp>
**Status:** Execution complete

**Summary:**
- Files modified: <count>
- Decisions made: <count>
- Deviations: <count or "none">
```

3. Display completion message:
```
Execution complete. Proceeding to verification. (Phase 8 - not yet implemented)

Run /do:continue to proceed when verification is implemented.
```
```

7. Add Files section:
```markdown
### Files

- **Config:** `.do/config.json` - Read `active_task`
- **Task:** `.do/tasks/<active_task>` - Read context, write Execution Log and stage updates
```

The file should be ~120-150 lines.
  </action>
  <verify>
    <automated>grep -c "Step E" skills/do/references/stage-execute.md | grep -qE "^[4]" && echo "PASS: Steps E0-E3 present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Step E0" skills/do/references/stage-execute.md` returns 0
    - `grep -q "Step E3" skills/do/references/stage-execute.md` returns 0
    - `grep -q "AskUserQuestion" skills/do/references/stage-execute.md` returns 0 (D-18 hybrid)
    - `grep -q "inline text prompt" skills/do/references/stage-execute.md` returns 0 (D-18 fallback)
    - `grep -q "per D-20" skills/do/references/stage-execute.md` returns 0 (log format reference)
    - `grep -q "per D-21" skills/do/references/stage-execute.md` returns 0 (deviation handling reference)
    - `grep -q "stage: verification" skills/do/references/stage-execute.md` returns 0 (D-22 transition)
    - File has YAML frontmatter with name and description
  </acceptance_criteria>
  <done>
    - stage-execute.md exists with Steps E0-E3
    - Context clear decision implemented per D-18 (hybrid AskUserQuestion + inline fallback)
    - Execution logging format documented per D-20
    - Deviation handling documented per D-21
    - Stage transitions documented per D-22
  </done>
</task>

<task type="auto">
  <name>Task 2: Update task-template.md Execution Log format</name>
  <files>skills/do/references/task-template.md</files>
  <read_first>
    - skills/do/references/task-template.md (current file)
    - .planning/phases/07-context-decision-implementation/07-CONTEXT.md (D-20 execution log format)
  </read_first>
  <action>
Update `skills/do/references/task-template.md` to have the D-20 execution log format in the comment.

Find the Execution Log section (currently lines 95-106):
```markdown
## Execution Log

<!--
This section is populated during the implementation phase.
Each entry should include: timestamp, action taken, result.

Format:
### YYYY-MM-DD HH:MM
- Action: <what was done>
- Result: <outcome>
- Files: <files modified>
-->
```

Replace with:
```markdown
## Execution Log

<!--
This section is populated during the implementation phase (per D-20).

Entry format:
### YYYY-MM-DD HH:MM
**Files:**
- `path/to/file.ts` - Change summary

**Decisions:**
- Plan said X - chose approach Y because Z
- [If error] Tried A, failed because B, resolved with C

**Status:** In progress / Execution complete

Context decision is logged first:
### <timestamp>
**Context decision:** [AskUserQuestion|inline prompt] - user chose [Yes|No]

Final entry has summary:
### <timestamp>
**Status:** Execution complete

**Summary:**
- Files modified: <count>
- Decisions made: <count>
- Deviations: <count or "none">
-->
```

This makes the template consistent with stage-execute.md instructions.
  </action>
  <verify>
    <automated>grep -q "per D-20" skills/do/references/task-template.md && echo "PASS: D-20 reference found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "per D-20" skills/do/references/task-template.md` returns 0
    - `grep -q "Context decision:" skills/do/references/task-template.md` returns 0
    - `grep -q "Files modified:" skills/do/references/task-template.md` returns 0
    - `grep -q "Decisions:" skills/do/references/task-template.md` returns 0
    - Existing sections (Problem Statement, Clarifications, etc.) unchanged
  </acceptance_criteria>
  <done>
    - Execution Log comment matches D-20 format
    - Context decision logging format documented
    - Final summary format documented
    - Rest of template unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SKILL.md routing to reference stage-execute.md</name>
  <files>skills/do/SKILL.md</files>
  <read_first>
    - skills/do/SKILL.md (routing table from Plan 01)
    - skills/do/references/stage-execute.md (created in Task 1)
  </read_first>
  <action>
Update `skills/do/SKILL.md` routing table to replace the execution placeholder with the actual reference.

Find the routing table and Stage Reference Loading section (added in Plan 01).

1. Update the routing table rows for execution:

FROM:
```markdown
| refinement | stages.grilling: complete OR confidence >= threshold | Display: "Execution stage not yet available." (Plan 02 adds stage-execute.md) |
| execution | any | Display: "Execution stage not yet available." (Plan 02 adds stage-execute.md) |
```

TO:
```markdown
| refinement | stages.grilling: complete OR confidence >= threshold | @skills/do/references/stage-execute.md |
| execution | any | @skills/do/references/stage-execute.md |
```

2. Update the Stage Reference Loading section:

FROM:
```markdown
**For execution (refinement ready or execution stage):**
Display: "Execution stage not yet available. Run Plan 02 first." (stub until Plan 02 creates stage-execute.md)
```

TO:
```markdown
**For execution (refinement ready or execution stage):**
@skills/do/references/stage-execute.md
```

This completes the router pattern for the execution stage. Verification stage remains as display-only placeholder until Phase 8.
  </action>
  <verify>
    <automated>grep -c "@skills/do/references/stage-execute.md" skills/do/SKILL.md | grep -qE "^[1-9]" && echo "PASS: Reference to stage-execute.md found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@skills/do/references/stage-execute.md" skills/do/SKILL.md` returns 0
    - `grep -q "Execution stage not yet available" skills/do/SKILL.md` returns 1 (NOT found - replaced)
    - `grep -q "Verification not yet implemented" skills/do/SKILL.md` returns 0 (Phase 8 placeholder remains)
    - Routing table shows @reference for both refinement-ready and execution stages
  </acceptance_criteria>
  <done>
    - SKILL.md routing table references stage-execute.md for execution
    - Stage Reference Loading section updated with actual @reference
    - Verification stage remains as display-only placeholder (Phase 8)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Context clear implemented:** `grep -q "AskUserQuestion" skills/do/references/stage-execute.md`
2. **Fallback implemented:** `grep -q "inline text prompt" skills/do/references/stage-execute.md`
3. **D-20 log format:** `grep -q "Files:" skills/do/references/stage-execute.md`
4. **D-21 deviation handling:** `grep -q "Plan said:" skills/do/references/stage-execute.md`
5. **D-22 transitions:** `grep -q "stages.execution: complete" skills/do/references/stage-execute.md`
6. **Template updated:** `grep -q "per D-20" skills/do/references/task-template.md`
7. **Router updated:** `grep -q "@skills/do/references/stage-execute.md" skills/do/SKILL.md`
8. **File count:** `ls skills/do/references/stage-*.md | wc -l` returns 2 (stage-grill.md and stage-execute.md)
</verification>

<success_criteria>
- stage-execute.md implements complete execution flow (Steps E0-E3)
- Context clear uses hybrid approach per D-18 (AskUserQuestion with inline fallback)
- Execution logging follows D-20 format
- Deviation handling per D-21 (stop and ask on ANY deviation)
- Stage transitions per D-22 (execution -> verification)
- task-template.md Execution Log comment updated to match D-20 format
- SKILL.md routing table references stage-execute.md (no more placeholder)
</success_criteria>

<output>
After completion, create `.planning/phases/07-context-decision-implementation/07-02-SUMMARY.md`
</output>
