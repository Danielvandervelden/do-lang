---
phase: 10-debug-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/do/references/debug-template.md
  - skills/do/scripts/__tests__/debug-session.test.cjs
autonomous: true
requirements:
  - TS-12

must_haves:
  truths:
    - "Debug session file can be created with proper frontmatter structure"
    - "Debug template has all required sections (Current Focus, Symptoms, Eliminated, Evidence, Resolution)"
    - "Section update rules are documented (OVERWRITE vs APPEND-only)"
  artifacts:
    - path: "skills/do/references/debug-template.md"
      provides: "Debug session file template"
      contains: "status: gathering"
    - path: "skills/do/scripts/__tests__/debug-session.test.cjs"
      provides: "Unit tests for debug session management"
      exports: ["describe", "test"]
  key_links:
    - from: "skills/do/references/debug-template.md"
      to: "GSD DEBUG.md pattern"
      via: "section rules"
      pattern: "OVERWRITE|APPEND only|IMMUTABLE"
---

<objective>
Create debug session infrastructure: template file and test suite for session management.

Purpose: Establish the foundation for /do:debug by creating the template that defines debug session structure (per D-40, D-41, D-43) and tests that verify session lifecycle behavior (per TS-12.1, TS-12.2, TS-12.3).

Output: debug-template.md with GSD-compatible structure, test file with failing tests for TDD approach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-debug-mode/10-CONTEXT.md
@.planning/phases/10-debug-mode/10-RESEARCH.md

<interfaces>
<!-- Key patterns to follow from existing implementation -->

From skills/do/references/task-template.md (pattern to mirror):
```yaml
---
id: {{TASK_ID}}
created: {{CREATED_AT}}
updated: {{CREATED_AT}}
description: "{{DESCRIPTION}}"
stage: refinement
stages:
  refinement: in_progress
  grilling: pending
  execution: pending
  verification: pending
  abandoned: false
confidence:
  score: {{CONFIDENCE_SCORE}}
  factors:
    context: {{CONTEXT_FACTOR}}
---
```

From skills/do/scripts/__tests__/load-task-context.test.cjs (test pattern):
```javascript
const { test, describe } = require("node:test");
const assert = require("node:assert");
const path = require("path");
```

From ~/.claude/get-shit-done/templates/DEBUG.md (section rules):
- Current Focus: OVERWRITE entirely on each update
- Symptoms: IMMUTABLE after gathering
- Eliminated: APPEND only
- Evidence: APPEND only
- Resolution: OVERWRITE as understanding evolves
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create debug-template.md reference file</name>
  <files>skills/do/references/debug-template.md</files>
  <read_first>
    - /Users/globalorange/.claude/get-shit-done/templates/DEBUG.md (canonical GSD debug pattern)
    - /Users/globalorange/workspace/github-projects/do/skills/do/references/task-template.md (template pattern to mirror)
    - /Users/globalorange/workspace/github-projects/do/.planning/phases/10-debug-mode/10-CONTEXT.md (D-40, D-41, D-43 decisions)
  </read_first>
  <behavior>
    - Template has YAML frontmatter with: status, trigger, created, updated, current_hypothesis, task_ref
    - Status field supports values: gathering | investigating | fixing | verifying | awaiting_human_verify | resolved
    - Template has 5 sections: Current Focus, Symptoms, Eliminated, Evidence, Resolution
    - Each section has clear update rules documented (OVERWRITE/APPEND/IMMUTABLE)
    - Filename format documented as YYMMDD-<slug>.md
  </behavior>
  <action>
Create `skills/do/references/debug-template.md` following the GSD DEBUG.md structure:

**Frontmatter (per D-41):**
```yaml
---
status: gathering
trigger: "{{TRIGGER}}"
created: {{CREATED_AT}}
updated: {{CREATED_AT}}
current_hypothesis: null
task_ref: null
---
```

**Sections (per D-43):**
1. `## Current Focus` - OVERWRITE on each update. Fields: hypothesis, test, expecting, next_action
2. `## Symptoms` - IMMUTABLE after gathering. Fields: expected, actual, errors, reproduction, started
3. `## Eliminated` - APPEND only. Format: `- hypothesis: ... evidence: ... timestamp: ...`
4. `## Evidence` - APPEND only. Format: `- timestamp: ... checked: ... found: ... implication: ...`
5. `## Resolution` - OVERWRITE. Fields: root_cause, fix, verification, files_changed

Add frontmatter metadata at top:
```yaml
---
name: debug-template
description: Template for debug session files in .do/debug/. Follows GSD debug pattern with scientific method workflow.
---
```

Include section rules as HTML comments within each section explaining update semantics.
  </action>
  <verify>
    <automated>grep -c "status: gathering" skills/do/references/debug-template.md && grep -c "OVERWRITE" skills/do/references/debug-template.md && grep -c "APPEND only" skills/do/references/debug-template.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at skills/do/references/debug-template.md
    - Contains `status: gathering | investigating | fixing | verifying | awaiting_human_verify | resolved` in frontmatter description
    - Contains `trigger: "{{TRIGGER}}"` placeholder
    - Contains `current_hypothesis: null` field
    - Contains `task_ref: null` field
    - Contains `## Current Focus` section with `<!-- OVERWRITE` comment
    - Contains `## Symptoms` section with `<!-- IMMUTABLE` comment
    - Contains `## Eliminated` section with `<!-- APPEND only` comment
    - Contains `## Evidence` section with `<!-- APPEND only` comment
    - Contains `## Resolution` section with `<!-- OVERWRITE` comment
    - Contains `hypothesis:`, `test:`, `expecting:`, `next_action:` fields under Current Focus
    - Contains `expected:`, `actual:`, `errors:`, `reproduction:`, `started:` fields under Symptoms
  </acceptance_criteria>
  <done>debug-template.md exists with all 5 sections, YAML frontmatter with status/trigger/timestamps/current_hypothesis/task_ref, and documented section update rules matching GSD pattern</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create debug-session.test.cjs test file</name>
  <files>skills/do/scripts/__tests__/debug-session.test.cjs</files>
  <read_first>
    - /Users/globalorange/workspace/github-projects/do/skills/do/scripts/__tests__/load-task-context.test.cjs (test pattern)
    - /Users/globalorange/workspace/github-projects/do/.planning/phases/10-debug-mode/10-RESEARCH.md (test requirements TS-12.1, TS-12.2, TS-12.3)
    - /Users/globalorange/workspace/github-projects/do/.planning/phases/10-debug-mode/10-CONTEXT.md (D-45, D-46 decisions)
  </read_first>
  <behavior>
    - Test file uses node:test and node:assert (established pattern)
    - Tests cover: createDebugSession, parseDebugFile, STATUS_TRANSITIONS, checkActiveDebug
    - Tests verify status transition validity (gathering->investigating, investigating->fixing, etc.)
    - Tests verify active session blocking logic
    - Tests use stub implementations that throw "Module not implemented" until Plan 02
  </behavior>
  <action>
Create `skills/do/scripts/__tests__/debug-session.test.cjs` with test structure:

```javascript
#!/usr/bin/env node

/**
 * Tests for debug session management
 *
 * Uses Node.js built-in test runner (Node 16.7+)
 * Run: node --test skills/do/scripts/__tests__/debug-session.test.cjs
 */

const { test, describe } = require("node:test");
const assert = require("node:assert");
const path = require("path");
```

**Test suites to create:**

1. `describe("STATUS_TRANSITIONS")` - Tests per D-42:
   - `test("gathering can transition to investigating")` - valid transition
   - `test("investigating can transition to fixing or verifying")` - valid transitions
   - `test("fixing can transition to verifying")` - valid transition
   - `test("verifying can transition to investigating or awaiting_human_verify")` - valid transitions
   - `test("awaiting_human_verify can transition to resolved or investigating")` - valid transitions
   - `test("resolved is terminal state")` - no valid transitions from resolved
   - `test("cannot skip from gathering to fixing")` - invalid transition

2. `describe("createDebugSession")` - Tests per D-40:
   - `test("creates filename in YYMMDD-slug format")` - format check
   - `test("generates slug from first 5 words of trigger")` - slug generation
   - `test("handles special characters in trigger")` - kebab-case, remove specials

3. `describe("parseDebugFile")` - Tests per D-43:
   - `test("parses frontmatter fields correctly")` - status, trigger, timestamps
   - `test("extracts current hypothesis")` - current_hypothesis field
   - `test("parses Current Focus section")` - hypothesis, test, expecting, next_action

4. `describe("checkActiveDebug")` - Tests per D-45, D-46:
   - `test("returns active: false when no active debug")` - no config entry
   - `test("returns active: true with status and hypothesis")` - active session
   - `test("returns stale: true when file missing")` - stale reference

All tests should have stub implementations that throw "Module not implemented" initially. The actual implementation comes in Plan 02.
  </action>
  <verify>
    <automated>node --test skills/do/scripts/__tests__/debug-session.test.cjs 2>&1 | grep -c "Module not implemented" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at skills/do/scripts/__tests__/debug-session.test.cjs
    - Contains `require("node:test")` and `require("node:assert")`
    - Contains `describe("STATUS_TRANSITIONS"` with at least 5 test cases
    - Contains `describe("createDebugSession"` with at least 3 test cases
    - Contains `describe("parseDebugFile"` with at least 3 test cases
    - Contains `describe("checkActiveDebug"` with at least 3 test cases
    - Running `node --test` shows failing tests with "Module not implemented" errors
    - Tests reference expected function signatures: STATUS_TRANSITIONS, createDebugSession, parseDebugFile, checkActiveDebug
  </acceptance_criteria>
  <done>Test file exists with 4 describe blocks covering STATUS_TRANSITIONS, createDebugSession, parseDebugFile, and checkActiveDebug, all tests fail with "Module not implemented"</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. `ls skills/do/references/debug-template.md` - file exists
2. `ls skills/do/scripts/__tests__/debug-session.test.cjs` - file exists
3. `grep -c "status:" skills/do/references/debug-template.md` - returns at least 1
4. `node --test skills/do/scripts/__tests__/debug-session.test.cjs` - tests run (will fail with "not implemented")
</verification>

<success_criteria>
- debug-template.md created with GSD-compatible structure (5 sections, frontmatter, update rules)
- debug-session.test.cjs created with test stubs for session management functions
- Template follows D-40, D-41, D-43 decisions exactly
- Tests cover TS-12.1, TS-12.2, TS-12.3 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/10-debug-mode/10-01-SUMMARY.md`
</output>
